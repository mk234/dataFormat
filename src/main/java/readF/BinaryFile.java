package readF;

import java.io.*;

public class BinaryFile {
/*
    void createBinaryFile() throws IOException {
        Node node = new Node("Page1", new Page(210, 297, new String[]{"fg"}), new FlowArea(10, 10, 80, 50), new Rectangle(5, 70, 200, 2, new Color(0, 0, 0)));
        FileOutputStream fos = new FileOutputStream("data.data");
        ObjectOutputStream oos = new ObjectOutputStream(fos);
        oos.writeObject(node);
        oos.close();
    }

    void readBinaryFile() throws IOException, ClassNotFoundException {
        File file = new File("data.data");
        FileInputStream fis = new FileInputStream(file);
        ObjectInputStream ois = new ObjectInputStream(fis);
        Node object = (Node) ois.readObject();
        System.out.println(object.toString());
    }*/

}